% dilworth.tex

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{center}
    \blue{\large A Decomposition Theorem for Partially Ordered Sets}
  \end{center}

  \begin{columns}
    \column{0.50\textwidth}
      \fig{width = 0.70\textwidth}{figs/Dilworth}
      \begin{center}
        {\small \teal{Robert Palmer Dilworth (1914 -- 1993)}}
      \end{center}
    \column{0.50\textwidth}
      \fig{width = 0.95\textwidth}{figs/dilworth-theorem-paper}
  \end{columns}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{center}
    \purple{\Large ``Deep'' Theorems}
  \end{center}

  \begin{columns}
    \column{0.50\textwidth}
      \pause
      \fig{width = 0.80\textwidth}{figs/connection}
    \column{0.50\textwidth}
      \pause
      \fig{width = 0.80\textwidth}{figs/connection}
  \end{columns}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{definition}[可比与不可比 (Comparable/Incomparable)]
    设 $(X, \preceq)$ 是偏序集。
    对 $a, b \in X$,

    \begin{description}
      \item[可比的 (Comparable):]
        \[
          a \preceq b \lor b \preceq a
        \]
      \item[不可比的 (Incomparable):]
        \[
          \lnot(a \preceq b \lor b \preceq a)
        \]
    \end{description}
  \end{definition}

  \fig{width = 0.50\textwidth}{figs/Hasse-Powerset}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{definition}[链与反链 (Chain; Antichain)]
    设 $(X, \preceq)$ 是偏序集。

    \begin{itemize}
      \setlength{\itemsep}{6pt}
      \item 设 $S \subseteq X$ 且 $S$ 中元素两两\blue{可比}, 则称 $S$ 是\red{链}。
      \item 设 $S \subseteq X$ 且 $S$ 中元素两两\blue{不可比}, 则称 $S$ 是\red{反链}。
    \end{itemize}
  \end{definition}

  \begin{center}
    注意: 单元素集合既是链, 也是反链
  \end{center}

  \fig{width = 0.50\textwidth}{figs/Hasse-Powerset}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{definition}[宽度 (Width)]
    设 $(X, \preceq)$ 是偏序集。
    \[
      \operatorname{Width}(X) =
      \max\{|S|: S \subseteq X \text{ is an antichain}\}.
    \]
  \end{definition}

  \fig{width = 0.50\textwidth}{figs/Hasse-Powerset}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{definition}[链分解 (Chain Decomposition)]
    设 $(X, \preceq)$ 是偏序集。
    对于 $C_{1}, C_{2}, \dots, C_{k} \subseteq X$, 如果
    \begin{enumerate}
      \setlength{\itemsep}{6pt}
      \item 每个 $C_{i}$ 都是\red{\bf 链};
      \item $C_{1}, C_{2}, \dots, C_{k}$ \red{\bf 互不相交};
      \item $X = \bigcup_{i=1}^{k} C_{i}$,
    \end{enumerate}
    则称 $\set{C_{1}, C_{2}, \dots, C_{k}}$
    是 $X$ 的\blue{\bf 链分解}。
  \end{definition}

  \fig{width = 0.40\textwidth}{figs/Hasse-Powerset}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \[
    \uncover<2->{\set{\blue{\set{x}}, \red{\set{y}}, \cyan{\set{z}}}}
    \qquad
    \uncover<3->{\set{\teal{\set{x, y}}, \brown{\set{y, z}}, \violet{\set{x, z}}}}
  \]
  \fig{width = 0.50\textwidth}{figs/Hasse-Powerset}
  \[
    \uncover<4->{\set{\blue{\set{\emptyset, \set{x}, \set{x, y}, \set{x, y, z}}},
      \red{\set{\set{y}, \set{y, z}}}, \cyan{\set{\set{z}, \set{x, z}}}}}
  \]
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \fig{width = 0.50\textwidth}{figs/lattice-integer}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \fig{width = 0.30\textwidth}{figs/division-6}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \fig{width = 0.60\textwidth}{figs/powerset}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \fig{width = 0.70\textwidth}{figs/60-poset}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \fig{width = 0.60\textwidth}{figs/set-partition-refinement}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{theorem}[Dilworth's Theorem]
    Let $(X,\le)$ be a finite poset. Then
    \[
      \operatorname{width}(X) =
      \min\{\text{number of chains in a chain decomposition of }X\}.
    \]
  \end{theorem}
    % \red{最大}\blue{反链}的大小 = \red{最小}\blue{链分解}中链的条数

  \pause
  \begin{center}
    \teal{\bf Antichains:} independence or concurrency
    \fig{width = 0.30\textwidth}{figs/set-partition-refinement}
    \vspace{-0.30cm}
    \teal{\bf Chain decompositions:} serial pipelines
  \end{center}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{center}
    \red{\large ``本课程迄今最复杂的证明''}
  \end{center}

  \begin{columns}
    \column{0.50\textwidth}
      \fig{width = 0.80\textwidth}{figs/lost}
      \begin{center}
        迷失在证明的迷宫中
      \end{center}
    \column{0.50\textwidth}
      \fig{width = 0.80\textwidth}{figs/chilun}
      \begin{center}
        环环相扣, 步步为营
      \end{center}
  \end{columns}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{theorem}[Dilworth's Theorem]
    Let $(X,\le)$ be a finite poset. Then
    \[
      \operatorname{width}(X) =
      \min\{\text{number of chains in a chain decomposition of }X\}.
    \]
  \end{theorem}

  \pause
  \vspace{2em}
  \begin{lemma}[$\ge$: \violet{Lower Bound} of Chain Decomposition Size]
    \[
      \text{minimum chain-decomposition size} \ge \operatorname{width}(X).
    \]
  \end{lemma}

  \vspace{1em}
  \begin{lemma}[$\le$: \violet{Upper Bound} of Chain Decomposition Size]
    \[
      \text{minimum chain-decomposition size} \le \operatorname{width}(X).
    \]
  \end{lemma}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{lemma}[$\ge$: \violet{Lower Bound} of Chain Decomposition Size]
    \[
      \text{minimum chain-decomposition size} \ge \operatorname{width}(X).
    \]
  \end{lemma}

  \begin{columns}
    \column{0.50\textwidth}
      \fig{width = 0.80\textwidth}{figs/Hasse-Powerset}
    \column{0.50\textwidth}
      \fig{width = 0.60\textwidth}{figs/so-easy}
  \end{columns}

  \pause
  \begin{proof}
    \begin{center}
      No chain can contain more than one element of an antichain.

      \pause
      \vspace{0.30cm}
      \red{By the Pigeonhole Principle.}
    \end{center}
  \end{proof}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{lemma}[$\le$: \violet{Upper Bound} of Chain Decomposition Size]
    \[
      \text{minimum chain-decomposition size} \le \operatorname{width}(X).
    \]
  \end{lemma}

  \begin{columns}
    \column{0.50\textwidth}
      \fig{width = 0.80\textwidth}{figs/Hasse-Powerset}
    \column{0.50\textwidth}
      \[
        \purple{\operatorname{width}(X) \triangleq k}
      \]
  \end{columns}

  \pause
  \begin{center}
    \brown{\it Suffices} to show:
    there \red{\it exists} a chain decomposition with $\le k$ chains.

    \pause
    \vspace{1em}
    To \blue{\it construct} a chain decomposition with $\le k$ chains, \\[3pt]
    \purple{based on the given largest antichain of size $k$}.

    % \pause
    % \vspace{1em}
    % Each chain contains \blue{\it exactly one} element
    % of the largest antichain.
  \end{center}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{center}
    To \blue{\it construct} a chain decomposition with $k$ chains, \\[3pt]
    \purple{based on the given largest antichain of size $k$}.
  \end{center}

  \begin{columns}
    \column{0.50\textwidth}
      \fig{width = 0.90\textwidth}{figs/Hasse-Powerset}
    \column{0.50\textwidth}
      \fig{width = 0.90\textwidth}{figs/powerset}
  \end{columns}

  \pause
  \begin{center}
    \red{\it Every} largest antichain is exactly
    the set of \blue{all minimal elements}, \\[3pt]
    or exactly the set of \blue{all maximal elements}.
  \end{center}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{lemma}[$\le$: \violet{Upper Bound} of Chain Decomposition Size]
    \[
      \text{minimum chain-decomposition size} \le \operatorname{width}(X).
    \]
  \end{lemma}

  \pause
  {\small
  \begin{proof}
    \begin{center}
      \red{By induction on the size $|X|$ of the poset.}
    \end{center}

    \pause
    \begin{description}[Inductive Hypothesis:]
      \setlength{\itemsep}{8pt}
      \item[Base Case:] $|X| = 1$; It holds obviously.
      \pause
      \item[Inductive Hypothesis:]
        Suppose it holds for all posets
        of size $2 \le |X| < n$.
      \pause
      \item[Inductive Step:]
        Consider a poset $X$ of size $|X| = n$.
        \vspace{0.20cm}
        \begin{itemize}
          \setlength{\itemsep}{6pt}
          \pause
          \item Suppose $A$ is a largest antichain with $k$ elements.
          \pause
          \item To construct a chain decomposition with $\le k$ chains.
        \end{itemize}
    \end{description}
  \end{proof}}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{lemma}[$\le$: Upper Bound of Chain Decomposition Size]
    \[
      \text{minimum chain-decomposition size} \le \operatorname{width}(X).
    \]
  \end{lemma}

  \begin{proof}
    \begin{description}
      \item[Case 1:]
        \red{\it Every} largest antichain is
        exactly the set of \blue{all minimal elements},
        or exactly the set of \blue{all maximal elements}.
    \end{description}

      \begin{columns}
        \column{0.50\textwidth}
          \fig{width = 0.75\textwidth}{figs/Hasse-Powerset}
        \column{0.50\textwidth}
          \fig{width = 0.90\textwidth}{figs/powerset}
      \end{columns}

    \begin{description}
      \item[Case 2:]
        There \red{\it exists} a largest antichain
        that is \blue{\it neither} the set of all minimal elements
        \blue{\it nor} the set of all maximal elements.
    \end{description}
  \end{proof}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{lemma}[$\le$: \violet{Upper Bound} of Chain Decomposition Size]
    \[
      \text{minimum chain-decomposition size} \le \operatorname{width}(X).
    \]
  \end{lemma}

  \begin{proof}
    \begin{description}
      \item[Case 1:]
        \red{\it Every} largest antichain is
        exactly the set of \blue{all minimal elements},
        or exactly the set of \blue{all maximal elements}.
    \end{description}
    \fig{width = 0.40\textwidth}{figs/powerset}

      \pause
    \begin{center}
      Let \red{$M$} be a largest antichain of $X$,
      the set of \blue{all minimal elements} of $X$.

      \pause
      \vspace{0.30cm}
      Choose $m \in M$ and a \teal{\it maximal} chain $C^{m}$ starting from $m$.
      \[
        C^{m}: m = x_{1} \prec x_{2} \prec \cdots \prec x_{t}
      \]
    \end{center}
  \end{proof}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{proof}
    \[
      \fbox{$\red{C^{m}}: m = x_{1} \prec x_{2} \prec \cdots \prec x_{t}$}
    \]
    \fig{width = 0.40\textwidth}{figs/powerset}

    \begin{center}
      $x_{t}$ is a \teal{\it maximal} element of $X$

      \pause
      \vspace{0.30cm}
      Remove \red{$C^{m}$} from $X$ to get a smaller poset $X' = X \setminus C^{m}$.

      \pause
      \vspace{1em}
      \purple{\fbox{$\operatorname{Width}(X') \le k - 1$}}

      \pause
      \vspace{0.30cm}
      {\small
        By the \blue{inductive hypothesis},
        $X'$ can be decomposed into \purple{\fbox{$\le (k - 1)$}} chains.}

      \pause
      \vspace{0.30cm}
      {\small Adding \red{$C^{m}$} back gives a chain decomposition
      of $X$ with \purple{\fbox{$\le k$}} chains.}
    \end{center}
  \end{proof}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{lemma}[$\le$: \violet{Upper Bound} of Chain Decomposition Size]
    \[
      \text{minimum chain-decomposition size} \le \operatorname{width}(X).
    \]
  \end{lemma}

  \begin{proof}
    \begin{description}
      \item[Case 2:]
        There \red{\it exists} a largest antichain
        that is \blue{\it neither} the set of all minimal elements
        \blue{\it nor} the set of all maximal elements.
    \end{description}

    \fig{width = 0.30\textwidth}{figs/Hasse-Powerset}
    \pause
    \vspace{-0.30cm}
    \[
      X^{+} = \set{x \in X: \exists a \in A\; x \ge a}
      \qquad
      X^{-} = \set{x \in X: \exists a \in A\; x \le a}
    \]
    \pause
    \vspace{-0.30cm}
    \[
      X^{+} \subset X \quad X^{-} \subset X
      \pause\qquad
      X^{+} \cup X^{-} = X \pause \quad X^{+} \cap X^{-} = A
    \]
    \pause
    \vspace{-0.30cm}
    \[
      \operatorname{Width}(X^{+}) = k
      \qquad
      \operatorname{Width}(X^{-}) = k
    \]
  \end{proof}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{proof}
    \vspace{-0.50cm}
    \[
      X^{+} = \set{x \in X: \exists a \in A\; x \ge a}
      \qquad
      X^{-} = \set{x \in X: \exists a \in A\; x \le a}
    \]
    \vspace{-0.80cm}
    \fig{width = 0.30\textwidth}{figs/Hasse-Powerset}
    \vspace{-0.30cm}
    {\small
    \pause
    \vspace{-0.30cm}
    \[
      \operatorname{Width}(X^{+}) = k
      \qquad\qquad
      \operatorname{Width}(X^{-}) = k
    \]}
    \pause
    \vspace{-0.60cm}
    \begin{center}
      {\small
      By the \blue{inductive hypothesis},
      $X^{+}$ ($X^{-}$) can be decomposed into \purple{\fbox{$\le k$}} chains.}

      \pause
      \vspace{0.50cm}
      {\small
      Actually, $X^{+}$ ($X^{-}$) can be decomposed into \purple{\fbox{$= k$}} chains, \\[3pt]
      each of which contains exactly one element of $A$.}

      \pause
      \vspace{0.50cm}
      {\small Concatenating the chain decompositions of $X^{+}$ and $X^{-}$ \\[3pt]
      gives a chain decomposition of $X$ with \purple{\fbox{$= k$}} chains.}
    \end{center}
  \end{proof}
\end{frame}
%%%%%%%%%%%%%%%